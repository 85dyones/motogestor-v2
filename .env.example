# Database configuration
POSTGRES_USER=motogestor
POSTGRES_PASSWORD=change-me-to-strong-password
POSTGRES_DB=motogestor_prod
POSTGRES_HOST=postgres

# If using an external/managed database, set DATABASE_URL instead:
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Authentication and security
JWT_SECRET_KEY=change-me-to-very-strong-random-key

# AI Service (if enabled)
OPENAI_API_KEY=sk-your-openai-key-here

# Application environment
APP_ENV=production
LOG_LEVEL=INFO

# Optional: Redis configuration (if needed for caching/sessions)
REDIS_URL=redis://redis:6379/0

# Optional: Application URLs (for CORS, etc.)
FRONTEND_URL=https://your-domain.com
API_GATEWAY_URL=https://your-domain.com/api
